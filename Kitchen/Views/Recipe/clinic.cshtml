<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">
<style>
    :root {
        --primary-dark: #034c53;
        --primary: #007074;
        --accent: #f38c79;
        --accent-light: #ffc1b4;
    }

    body {
        font-family: "Tajawal", sans-serif;
        background: linear-gradient(135deg, #f5f7fa 0%, #e4e8eb 100%);
        min-height: 100vh;
    }

    .navbar-brand {
        font-weight: 700;
        color: var(--primary-dark);
    }

    .logo-container {
        display: flex;
        align-items: center;
    }

    .logo-text {
        margin-right: 10px;
    }

    .logo-main {
        font-size: 1.8rem;
        font-weight: 700;
        color: var(--primary-dark);
        margin-bottom: 0;
    }

    .logo-subtitle {
        transition: all 0.3s ease;
        white-space: nowrap;
    }

    .logo-img {
        width: 60px;
        height: 60px;
        object-fit: cover;
        border-radius: 50%;
        transition: all 0.3s ease;
        animation: pulse 2s infinite;
    }


    .bg-gradient-primary {
        background: linear-gradient( 135deg, var(--primary-dark) 0%, var(--primary) 100% ) !important;
    }

    .btn-accent {
        background-color: var(--accent);
        color: white;
        border: none;
    }

        .btn-accent:hover {
            background-color: var(--primary);
            color: white;
        }

    .card {
        border: none;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
    }

        .card:hover {
            transform: translateY(-5px);
        }

    .card-header {
        background: linear-gradient( 135deg, var(--primary) 0%, var(--primary-dark) 100% );
        color: white;
        border-radius: 10px 10px 0 0 !important;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .category-badge {
        background-color: var(--accent-light);
        color: var(--primary-dark);
        font-weight: 600;
    }

    footer {
        background: linear-gradient( 135deg, var(--primary-dark) 0%, var(--primary) 100% );
        color: white;
    }

    .input-group > :not(:first-child):not(.dropdown-menu):not(.valid-tooltip):not( .valid-feedback ):not(.invalid-tooltip):not(.invalid-feedback) {
        border-top-right-radius: 0;
        border-bottom-right-radius: 0;
        border-top-left-radius: 0.375rem;
        border-bottom-left-radius: 0.375rem;
    }

    .input-group > .form-control:not(:first-child) {
        border-top-left-radius: 0;
        border-bottom-left-radius: 0;
        border-top-right-radius: 0.375rem;
        border-bottom-right-radius: 0.375rem;
    }

    .recipe-code {
        background-color: rgba(255, 255, 255, 0.2);
        padding: 3px 8px;
        border-radius: 4px;
        font-family: monospace;
        font-size: 0.9rem;
    }

    .card-footer-buttons {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 10px;
    }

    .more-recipes {
        display: none;
    }

    .chemical-icon {
        background-color: rgba(255, 193, 180, 0.2);
        border-radius: 50%;
        width: 80px;
        height: 80px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 15px;
    }

    .hero-img {
        width: 100%;
        height: auto;
        max-height: 400px;
        object-fit: cover;
        border-radius: 10px;
        margin-top: 10px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
    }

    .logo-subtitle {
        font-weight: 500;
        letter-spacing: -0.5px;
        margin-top: -5px;
    }

    /* Add hover animations */
    .card {
        transition: all 0.3s ease-in-out;
        overflow: hidden;
    }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }

        .card img {
            transition: transform 0.5s ease;
        }

        .card:hover img {
            transform: scale(1.05);
        }

    .btn {
        transition: all 0.3s ease;
        margin: 5px 0;
    }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

    /* Hero buttons responsive styles */
    .hero-buttons {
        margin-top: 20px;
    }

        .hero-buttons .btn {
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

            .hero-buttons .btn:hover {
                transform: translateY(-3px);
                box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            }




    .chemical-icon:hover {
        transform: rotate(360deg);
        background-color: rgba(255, 193, 180, 0.4);
    }



    .fade-in-up {
        animation: fadeInUp 0.6s ease-out;
    }

   

    .category-badge {
        animation: pulse 2s infinite;
    }

</style>
<!-- Hero Section -->
<section class="hero-section">
    <div class="container py-5">
        <div class="row align-items-center">
            <div class="col-12 col-lg-6 order-2 order-lg-1 text-lg-right">
                <h1 class="display-4 fw-bold mb-4" style="color: var(--primary-dark)" data-aos="fade-up" data-aos-delay="100">
                    @if (ViewData["Direction"].ToString() == "rtl")
                    {
                        <text>القسم الصيدلاني</text>
                    }
                    else
                    {
                        <text>Pharmaceutical Section</text>
                    }
                    <br />
                    <span class="h3" style="color: var(--accent)">
                        @if (ViewData["Direction"].ToString() == "rtl")
                        {
                            <text>قسم صيدلي</text>
                        }
                        else
                        {
                            <text>Pharmaceutical Section</text>
                        }
                    </span>
                </h1>
                <p class="lead mb-4" data-aos="fade-up" data-aos-delay="200">
                    @if (ViewData["Direction"].ToString() == "rtl")
                    {
                        <text>اكتشف مجموعتنا المتنوعة من الأعشاب الطبية والأدوية العشبية والمستحضرات الصيدلانية. جميع منتجاتنا مصنعة وفق أعلى معايير الجودة والسلامة.</text>
                    }
                    else
                    {
                        <text>Explore our diverse collection of herbal medicines, herbal supplements, and pharmaceutical products. All our products are manufactured according to the highest standards of quality and safety.</text>
                    }
                </p>
                <div class="hero-buttons d-flex flex-column flex-sm-row gap-3 justify-content-center justify-content-lg-start" data-aos="fade-up" data-aos-delay="300">
                    <a href="#recipes" class="btn btn-accent btn-lg rounded-pill px-4">
                        @if (ViewData["Direction"].ToString() == "rtl")
                        {
                            <text>تصفح المنتجات</text>
                        }
                        else
                        {
                            <text>Browse Products</text>
                        }
                    </a>
                    <a href="#" class="btn btn-outline-secondary btn-lg rounded-pill px-4">
                        @if (ViewData["Direction"].ToString() == "rtl")
                        {
                            <text>المزيد من المعلومات</text>
                        }
                        else
                        {
                            <text>More Info</text>
                        }
                    </a>
                </div>
            </div>
            <div class="col-12 col-lg-6 order-1 order-lg-2 mb-4 mb-lg-0">
                <img src="~/assets/sectionparphc.jpg" class="hero-img" alt="صورة صيدلية" loading="eager" data-aos="fade-left" data-aos-delay="400" />
            </div>
        </div>
    </div>
</section>

<!-- Recipe Categories -->
<section class="py-5 bg-light">
    <div class="container">
        <div class="row g-4">
            <div class="col-6 col-md-6">
                <div class="card h-100 filter-category" data-category="herbal">
                    <div class="card-body text-center p-4">
                        <div class="chemical-icon">
                            <i class='bx bxs-leaf' style="font-size: 40px; color: var(--primary-dark);"></i>
                        </div>
                        <h4 class="card-title">
                            @if (ViewData["Direction"].ToString() == "rtl")
                            {
                                <text>اعشاب وعطارة</text>
                            }
                            else
                            {
                                <text>Herbal and Apothecary</text>
                            }
                        </h4>
                        <p class="card-text">
                            @if (ViewData["Direction"].ToString() == "rtl")
                            {
                                <text>منتجات طبيعية وأعشاب طبية عالية الجودة</text>
                            }
                            else
                            {
                                <text>High-quality herbal and apothecary products</text>
                            }
                        </p>
                    </div>
                </div>
            </div>
            <div class="col-6 col-md-6">
                <div class="card h-100 filter-category" data-category="medicine">
                    <div class="card-body text-center p-4">
                        <div class="chemical-icon">
                            <i class='bx bxs-capsule' style="font-size: 40px; color: var(--primary-dark);"></i>
                        </div>
                        <h4 class="card-title">
                            @if (ViewData["Direction"].ToString() == "rtl")
                            {
                                <text>ادوية</text>
                            }
                            else
                            {
                                <text>Medicine</text>
                            }
                        </h4>
                        <p class="card-text">
                            @if (ViewData["Direction"].ToString() == "rtl")
                            {
                                <text>مستحضرات صيدلانية وأدوية عالية الجودة</text>
                            }
                            else
                            {
                                <text>High-quality pharmaceutical products and medicines</text>
                            }
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



<section id="recipes" class="py-5" dir="@ViewData["Direction"]">
    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h2 style="color: var(--primary)">
                @if (ViewData["Direction"].ToString() == "rtl")
                {
                    @:منتجات صيدليه مميزة
                }
                else
                {
                    @:Distinctive pharmacy products
                }
            </h2>
        </div>

        <div class="row g-4">
            @foreach (var recipe in Model)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100">
                        <div class="card-header">
                            <span class="badge category-badge float-start">
                                @if (ViewData["Direction"].ToString() == "rtl")
                                {
                                    @recipe.SloganAr
                                }
                                else
                                {
                                    @recipe.SloganEn
                                }
                            </span>
                            <h5 class="mb-0">
                                @if (ViewData["Direction"].ToString() == "rtl")
                                {
                                    @recipe.TitleAr
                                }
                                else
                                {
                                    @recipe.TitleEn
                                }
                            </h5>
                        </div>
                        <img src="~/@recipe.ImagePath" alt="@recipe.TitleAr" class="card-img-top" />
                        <div class="card-body">
                            @if (ViewData["Direction"].ToString() == "rtl")
                            {
                                @recipe.DescriptionAr
                            }
                            else
                            {
                                @recipe.DescriptionEn
                            }
                        </div>
                        <div class="card-footer bg-transparent border-0 flex-column">
                            <div class="card-footer-buttons">
                                <div class="d-flex justify-content-between flex-row" style="width: 100%">
                                    <span class="fw-bold text-accent" style="color: var(--accent)">
                                        @recipe.Price.ToString("F2") $
                                    </span>
                                </div>
                                <a class="btn btn-sm btn-accent" style="width: 100%" href="/Recipe/EnterCode/@recipe.Id">
                                    @if (ViewData["Direction"].ToString() == "rtl")
                                    {
                                        @:تحميل
                                    }
                                    else
                                    {
                                        @:Download
                                    }
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</section>



<section class="py-5 bg-white text-white" dir="@ViewData["Direction"]">
    <div class="container py-4 text-center">
        <h2 class="display-5 fw-bold mb-4" style="color: var(--primary-dark)">
            @if (ViewData["Direction"]?.ToString() == "rtl")
            {
                @:هل أنت مستعد لتحويل إنتاجك الصيدلي؟
            }
            else
            {
                @:Are you ready to transform Your pharmaceutical production?
            }
        </h2>
        <p class="lead mb-5" style="color: var(--primary-dark)">
            @if (ViewData["Direction"]?.ToString() == "rtl")
            {
                @:انضم إلى آلاف الصناعيين والمهنيين  الذين يثقون بتركيباتنا لتحقيق محاصيل أفضل وزيادة في الإنتاج.
            }
            else
            {
                @:Join the thousands of industrialists and professionals who trust our formulas for better crops and increased production.
            }
        </p>
        <div class="d-flex justify-content-center gap-3">
            <a href="#"
               class="btn btn-light btn-lg rounded-pill px-4"
               style="color: var(--primary-dark); border: 1px solid var(--primary-dark); background-color: transparent;">
                @if (ViewData["Direction"]?.ToString() == "rtl")
                {
                    @:ابدأ الآن
                }
                else
                {
                    @:Get Started
                }
            </a>
            <a asp-action="contact"
               class="btn btn-outline-light btn-lg rounded-pill px-4"
               style="background-color: var(--primary-dark); color: white;">
                @if (ViewData["Direction"]?.ToString() == "rtl")
                {
                    @:اتصل بنا
                }
                else
                {
                    @:Contact Us
                }
            </a>
        </div>
    </div>
</section>

<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>
    AOS.init({
      duration: 800,
      easing: "ease-in-out",
      once: true,
      mirror: false,
    });

    // Add smooth scrolling
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", function (e) {
        e.preventDefault();
        document.querySelector(this.getAttribute("href")).scrollIntoView({
          behavior: "smooth",
        });
      });
    });

    document
      .getElementById("showMoreBtn")
      .addEventListener("click", function () {
        const moreRecipes = document.querySelector(".more-recipes");
        const btn = this;

        if (
          moreRecipes.style.display === "none" ||
          moreRecipes.style.display === ""
        ) {
          // Show more recipes with a smooth transition
          moreRecipes.style.display = "flex";
          moreRecipes.style.opacity = "0";
          setTimeout(() => {
            moreRecipes.style.transition = "opacity 0.5s ease";
            moreRecipes.style.opacity = "1";
          }, 10);
          btn.textContent = "عرض وصفات أقل";

          // Scroll to the newly displayed recipes
          setTimeout(() => {
            const firstHiddenRecipe = moreRecipes.querySelector(".card");
            if (firstHiddenRecipe) {
              firstHiddenRecipe.scrollIntoView({
                behavior: "smooth",
                block: "nearest",
              });
            }
          }, 100);
        } else {
          // Hide recipes with a smooth transition
          moreRecipes.style.transition = "opacity 0.5s ease";
          moreRecipes.style.opacity = "0";
          setTimeout(() => {
            moreRecipes.style.display = "none";
          }, 500);
          btn.textContent = "عرض جميع الوصفات";

          // Scroll back to the show more button
          btn.scrollIntoView({ behavior: "smooth", block: "nearest" });
        }
      });

    // Add responsive behavior for window resize
    window.addEventListener("resize", function () {
      // Adjust any elements that need special handling on resize
      const cardFooterButtons = document.querySelectorAll(
        ".card-footer-buttons"
      );
      if (window.innerWidth < 768) {
        cardFooterButtons.forEach((btn) => {
          btn.classList.add("flex-column");
          btn.classList.add("align-items-start");
        });
      } else {
        cardFooterButtons.forEach((btn) => {
          btn.classList.remove("flex-column");
          btn.classList.remove("align-items-start");
        });
      }
    });

    // Trigger resize event on load to apply initial responsive adjustments
    window.dispatchEvent(new Event("resize"));

    // Handle navbar responsiveness
    function adjustNavbar() {
      const navbarCollapse = document.querySelector(".navbar-collapse");
      const logoImg = document.querySelector(".logo-img");
      const logoMain = document.querySelector(".logo-main");
      const logoSubtitle = document.querySelector(".logo-subtitle");
      const heroButtons = document.querySelector(".hero-buttons");
      const logoContainer = document.querySelector(".logo-container");
      const logoText = document.querySelector(".logo-text");
      const textEnd = document.querySelector(".text-end");

      // Ensure logo container and text are always in a row
      if (logoContainer) {
        logoContainer.style.flexDirection = "row";
        logoContainer.style.alignItems = "center";
      }

      if (logoText) {
        logoText.style.flexDirection = "row";
        logoText.style.alignItems = "center";
      }

      if (textEnd) {
        textEnd.style.textAlign = "right";
      }

      if (window.innerWidth < 576) {
        // Mobile view
        logoImg.style.width = "40px";
        logoImg.style.height = "40px";
        logoMain.style.fontSize = "1.1rem";
        logoMain.style.whiteSpace = "nowrap";
        logoSubtitle.style.fontSize = "0.7rem";
        logoSubtitle.style.whiteSpace = "nowrap";

        if (navbarCollapse) {
          navbarCollapse.classList.add("navbar-mobile");
        }

        // Add animation to logo
        logoImg.style.animation = "pulse 2s infinite";

        // Adjust hero buttons for mobile
        const buttons = document.querySelectorAll(".hero-buttons .btn");
        buttons.forEach((btn) => {
          btn.style.width = "100%";
          btn.style.margin = "5px 0";
          btn.style.padding = "12px 20px";
          btn.style.fontSize = "1rem";
        });
      } else if (window.innerWidth < 768) {
        // Tablet view
        logoImg.style.width = "45px";
        logoImg.style.height = "45px";
        logoMain.style.fontSize = "1.2rem";
        logoMain.style.whiteSpace = "nowrap";
        logoSubtitle.style.fontSize = "0.75rem";
        logoSubtitle.style.whiteSpace = "nowrap";

        if (navbarCollapse) {
          navbarCollapse.classList.remove("navbar-mobile");
        }

        // Add animation to logo
        logoImg.style.animation = "pulse 2s infinite";

        // Adjust hero buttons for tablet
        const buttons = document.querySelectorAll(".hero-buttons .btn");
        buttons.forEach((btn) => {
          btn.style.width = "";
          btn.style.margin = "0 5px";
          btn.style.padding = "";
          btn.style.fontSize = "";
        });
      } else if (window.innerWidth < 992) {
        // Small desktop view
        logoImg.style.width = "50px";
        logoImg.style.height = "50px";
        logoMain.style.fontSize = "1.3rem";
        logoMain.style.whiteSpace = "nowrap";
        logoSubtitle.style.fontSize = "0.8rem";
        logoSubtitle.style.whiteSpace = "nowrap";

        if (navbarCollapse) {
          navbarCollapse.classList.remove("navbar-mobile");
        }

        // Add animation to logo
        logoImg.style.animation = "pulse 2s infinite";

        // Adjust hero buttons for desktop
        const buttons = document.querySelectorAll(".hero-buttons .btn");
        buttons.forEach((btn) => {
          btn.style.width = "";
          btn.style.margin = "0 5px";
          btn.style.padding = "";
          btn.style.fontSize = "";
        });
      } else {
        // Desktop view
        logoImg.style.width = "60px";
        logoImg.style.height = "60px";
        logoMain.style.fontSize = "1.5rem";
        logoMain.style.whiteSpace = "nowrap";
        logoSubtitle.style.fontSize = "1rem";
        logoSubtitle.style.whiteSpace = "nowrap";

        if (navbarCollapse) {
          navbarCollapse.classList.remove("navbar-mobile");
        }

        // Add animation to logo
        logoImg.style.animation = "pulse 2s infinite";

        // Adjust hero buttons for desktop
        const buttons = document.querySelectorAll(".hero-buttons .btn");
        buttons.forEach((btn) => {
          btn.style.width = "";
          btn.style.margin = "0 5px";
          btn.style.padding = "";
          btn.style.fontSize = "";
        });
      }
    }

    // Call the function on load and resize
    window.addEventListener("resize", adjustNavbar);
    document.addEventListener("DOMContentLoaded", adjustNavbar);

    // Download Modal logic
    const downloadModal = new bootstrap.Modal(
      document.getElementById("downloadModal")
    );
    document
      .querySelectorAll(".card-footer-buttons .btn-accent")
      .forEach((btn, idx) => {
        btn.addEventListener("click", function (e) {
          e.preventDefault();
          // Example: recipe files named recipe1.zip, recipe2.zip, ...
          const fileName = `recipe${idx + 1}.zip`;
          const link = document.getElementById("downloadRecipeLink");
          link.href = `downloads/${fileName}`;
          link.setAttribute("download", fileName);
          downloadModal.show();
        });
      });

    // Language switch logic
    const translations = {
      ar: {
        "nav-home": "الرئيسية",
        "nav-agriculture": "الزراعية",
        "nav-chemical": "الكيميائية",
        "nav-contact": "اتصل بنا",
        "hero-title": "وصفات التصنيع الكيميائي",
        "hero-desc":
          "تركيبات كيميائية دقيقة للصناعات المختلفة، مواد تنظيف، مستحضرات تجميل، مواد بناء، ومواد كيميائية متخصصة. مصممة وفق أعلى معايير الجودة.",
        "hero-btn1": "تصفح الوصفات",
        "hero-btn2": "المزيد من المعلومات",
      },
      en: {
        "nav-home": "Home",
        "nav-agriculture": "Agriculture",
        "nav-chemical": "Chemical",
        "nav-contact": "Contact",
        "hero-title": "Chemical Manufacturing Recipes",
        "hero-desc":
          "Precise chemical formulas for various industries, cleaning materials, cosmetics, building materials, and specialized chemicals. Designed to the highest quality standards.",
        "hero-btn1": "Browse Recipes",
        "hero-btn2": "More Info",
      },
    };

    function setLang(lang) {
      document.documentElement.lang = lang === "ar" ? "ar" : "en";
      document.documentElement.dir = lang === "ar" ? "rtl" : "ltr";
      for (const key in translations[lang]) {
        const el = document.querySelector(`[data-i18n="${key}"]`);
        if (el) el.textContent = translations[lang][key];
      }
      document.getElementById("langSwitch").textContent =
        lang === "ar" ? "EN" : "AR";
      localStorage.setItem("lang", lang);
    }

    document
      .getElementById("langSwitch")
      .addEventListener("click", function () {
        const current = localStorage.getItem("lang") || "ar";
        setLang(current === "ar" ? "en" : "ar");
      });

    window.addEventListener("DOMContentLoaded", function () {
      setLang(localStorage.getItem("lang") || "ar");
    });
</script>